SOURCES='package.json mongoose-connector.d.ts'
TARGET=node_modules/@sabbatical/mongoose-connector
rm -fr ${TARGET}
mkdir -p ${TARGET}
cp ${SOURCES} ${TARGET}
echo building library
tsc -p .
cp ./mongoose-connector.js ${TARGET} 
echo building tests
tsc -p tsconfig.tests.json
